o Profile story component.
    - Fix the resize bug (back burner)
o Admin interface.
    - Confirm save on exit
o Map scaling issue (it doesn't fill the window if it's not the expected size).
o Make pin coordinates percentage based instead of pixel based.
o Make the site highlights look more like speech bubbles instead of just rounded rectangles.
o Add info message if there is no video (similar to the no images message).
